name: security-check

on:
  push:

jobs:
  scan:
    name: Run Salus scan
    runs-on: ubuntu-latest
    steps:
    -
      uses: actions/checkout@master
      with:
        ref: ${{ github.ref }}
    -
      name: Run scam
      run: |
        bash salus.sh

